{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and run",
            "type": "shell",
            "command": "ts-node index.ts",
            "options": {
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
